
{% if site['postSliders'] %}
<div id="carouselExampleControls" class="carousel slide" data-ride="carousel" style="margin: -37px 0 30px 0;">
    <div class="carousel-inner">
        {% for key, slider in site['postSliders'] %}
        <div class="carousel-item {% if key == 0 %} active {% endif %}">
            <img class="d-block w-100" src="{{ site['postsImgUrl'] }}-slider/{{ slider['image'] }}" alt="First slide">
        </div>
        {% endfor %}
    </div>
    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>
{% endif %}

<div class="container">
    <div class="row">
        {% if site['post'] %}
            <div class="col-md-2"></div>
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <img class="card-img-top img-fluid" src="{{ site['postsImgUrl'] }}/{{ site['post']['image'] }}" >
                    <div class="card-body">
                        <h4 class="card-text">
                            {{ site['post']['title'] }}
                        </h4>
                        <div>
                            {{ site['post']['content'] | raw }}
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-2"></div>
        {% endif %}

        {% if site['posts'] %}
            {% for post in site['posts'] %}
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        <img class="card-img-top img-fluid" src="{{ site['postsImgUrl'] }}/{{ post['image'] }}" >
                        <div class="card-body">
                            <p class="card-text">
                                {{ post['title']|length > 50 ? post['title']|slice(0, 50) ~ '...' : post['title']  }}
                            </p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a href="{{ site['url'] }}?details={{ post['id'] }}" class="btn btn-sm btn-outline-secondary">View</a>
                                </div>
                                <small class="text-muted">9 mins</small>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% endif %}
    </div>
</div>
